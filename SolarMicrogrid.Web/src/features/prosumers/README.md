# Prosumers feature

Shared feature boundary for Prosumer-specific pages and API state. `ProsumersController` is currently empty, so this folder deliberately contains no invented production integration. The existing bounded eligible-Prosumer search belongs to the user API and is reused only where its confirmed contract applies.
