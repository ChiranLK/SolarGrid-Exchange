# Reservations feature

Component 3 reservation list, detail, creation, update, cancellation, approval, and rejection UI. It consumes `/api/reservations` DTOs and server-derived `allowedActions` without recreating lifecycle authorization in React.
